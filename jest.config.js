// jest.config.js
module.exports = {
  collectCoverage : true,

    // testEnvironment: "jsdom",

    // setupFiles: ["<rootDir>/checkout.test.js"],
    transform: {
      "^.+\\.jsx?$": "babel-jest"
    }
  };
  